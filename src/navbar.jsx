import React from 'react';

const Navbar = () => {
  return (
    <nav>
      <ul>
        <li>
          <button>Export</button>

        </li>
        <li>
          <button>Import</button>
        </li>

      </ul>

    </nav>
  );
};

export default Navbar;
